---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: quay.io/hellofresh/scm-concourse-docker-compose

inputs:
  - name: source-code

outputs:
  - name: docker

run:
  path: docker-up
  dir: source-code
